# wml-backup

Backup local das pastas antigas usadas como referência durante a migração.

Esses arquivos não fazem parte do runtime do aplicativo principal. O app usa apenas `wml-store` e `wml-backend`.
